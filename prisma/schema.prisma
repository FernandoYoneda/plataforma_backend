// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
  role     String
}

model Settings {
  id          Int    @id
  sector      String
  nameOrStore String
}

model Order {
  id          Int      @id @default(autoincrement())
  item        String
  qty         Int
  notes       String?
  sector      String?
  nameOrStore String?
  status      String   @default("aberto") // aberto | em_andamento | finalizado
  response    String?                       // resposta do responsável
  done        Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model TiTicket {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  sector      String?
  nameOrStore String?
  status      String   @default("aberto")
  response    String?
  done        Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
